# ── NEVER commit these ───────────────────────────────────────────
.secrets_cache.json
*.pyc
__pycache__/
.env
.env.*

# docs/ contains chat.js which has a live API token baked in.
# It must NEVER be committed — GitHub push protection will block it.
# docs/ is built by Actions on the runner and uploaded directly to
# GitHub Pages as an artifact. It never touches the git repo.
docs/